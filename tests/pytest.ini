[pytest]
markers =
    slow: marks tests as slow (boot QEMU, ~30s-5min each)
    benchmark: performance benchmarks with structured output
    binre: binary-RE differential tests of rebuilt IRIX binaries (most also 'slow')
